[unstable]
build-std = ["core", "compiler_builtins"]
build-std-features = ["compiler-builtins-mem"]
json-target-spec = true

[build]
target = "x86_64-trinix.json"

[target.'cfg(target_os = "none")']
runner = "qemu-system-x86_64 -drive format=raw,file="
